<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
				"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.book.mapper.MemberMapper">
	<!-- <select id="getAllList" resultType="kr.board.entity.BoardVO">
    	select * from board order by idx desc
    </select> -->
    
    <insert id="join" parameterType="kr.book.entity.Member">
	    insert into board(MBR_NBR,MBR_ID,MBR_PW,MBR_NAME,MBR_CNT,MBR_SHOP,MBR_POS,MBR_MAIL)
		values(mem_seq.nextval,#{id},#{password},#{name},#{phone},#{shop},#{position},#{email})
	</insert>
    
	
   
</mapper>